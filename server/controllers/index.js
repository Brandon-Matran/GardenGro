
// const db = {};

// db.Sequelize = Sequelize;
// db.sequelize = sequelize;

// db.journals = require('../models/journal.model.js')(sequelize, Sequelize);


// Object.assign(db, require('../models'));
// db.sequelize.sync().then(() => {
//     console.log("Database Synced");
//     app.listen(PORT, () => {
//         console.log(`Server running on ${PORT}`)
//     })});

// module.exports = db;
